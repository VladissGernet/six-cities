import { PlacesSortingValueType } from '../../types/general';
import { Offers } from '../../types/offers';

export function sortOffers(offers: Offers, option: PlacesSortingValueType) {
  // TODO, отсортировать список
  console.log(offers);
  console.log(option);
}
